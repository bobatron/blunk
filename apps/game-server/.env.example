# From your LiveKit Cloud project settings (Settings > Keys)
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# The deployed web app's origin, for CORS on the /token endpoint.
# Use * while developing locally; set to the real Vercel URL in production.
WEB_ORIGIN=*

PORT=8080
