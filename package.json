{
  "name": "blunk",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace game-server",
    "build": "npm run build --workspace web && npm run build --workspace game-server",
    "lint": "npm run lint --workspace web && npm run lint --workspace game-server"
  }
}
